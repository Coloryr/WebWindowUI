<Project>

  <!--
    WebWindowUI 共享构建目标：把「模型 → proto 生成器」「前端构建编排」「wwwroot 传递复制」做成可复用逻辑。
    两种接入方式：
      仓库模式：根 Directory.Build.targets 统一导入（对齐包模式 buildTransitive；ProjectReference
              不透传 .targets 导入，故由根文件兜底，详见根 Directory.Build.targets）；
      包模式：WebWindowUI nuget 包的 build/WebWindowUI.targets 在 PackageReference 时被自动导入
              （标记包 WebWindowUI.Backend / WebWindowUI.Frontend 依赖核心包，引用它们即带回本文件），
              生成器随包内嵌在 tools/net10.0/（$(MSBuildThisFileDirectory)=包内 build/ 目录，父级即包根）。
    纯后端工程（无模型/无前端）即使被自动导入 targets 也是空操作：各目标以存在性条件自行短路，
    仅 CheckFrontend 对「声明了模型、有 wwwroot 却缺前端窗口页面」的工程报错。

    角色判定（取代内容启发式，由「引用哪个标记类库」证明，而非目录名/文件名）：
      WebWindowUI.Backend（后端模型库）  → 触发模型→proto/descriptor/TS 生成；
      WebWindowUI.Frontend（前端工程）   → 激活本工程自身的 vite 构建目标；
      两者都不引用（应用 exe）            → 构建前端（vite 直产本工程 wwwroot）+ 传递复制。
      仓库模式（ProjectReference 到标记库）与包模式（PackageReference 到标记包）统一按引用项判定，
      所以叫 Foo.Frontend 但没引用标记的工程不会被当作前端。

      MSBuild 求值限制（本文档多处按它规避）：
        - 求值期 PropertyGroup 的 Condition 不允许引用 item 列表/元数据（MSB4099/MSB4190/MSB4191）——
          角色判定放在 ItemGroup 层用 WithMetadataValue 过滤，下游 Target 条件（exec 期）直接引用项；
        - item transform @(I->'%(Meta)') 只在 item Include 里展开，属性赋值保持字面量——
          前端工程路径/目录都用「transform 转成新项」再经纯 @(Item) 进属性，不用属性函数叠路径；
        - 属性函数参数内的 item 引用不展开（GetDirectoryName('$(P)') 收到原始 @(...)）——
          目录在 item 层用 %(Directory) 派生，不依赖属性函数。
      角色信号 = %(Filename) 精确匹配（ProjectReference 是工程文件名，无扩展名剥壳）或 %(Identity)
      精确匹配（PackageReference 是包名）。包名 WebWindowUI.Backend 无扩展名时 %(Filename) 会把
      .Backend 当扩展名剥成 WebWindowUI，故包引用改按 Identity 精确匹配。

    消费方只需：
      1) 后端模型库工程引用 WebWindowUI.Backend，把模型类源码放在工程根目录、命名以 Model.cs 结尾
         （文件名即类名），即可自动被发现与生成：
         MainWindowModel.cs → 类 MainWindowModel，前端 descriptor 基名 main_window_model
         （ProtoBase 由类名自动推导：PascalCase → snake_case）。
         命名约定不适用时，可显式声明 <ModelProtoFile Include=... ProtoBase=... /> 覆盖自动扫描。
      2) 前端工程引用 WebWindowUI.Frontend，在 package.json 里提供 npm run build（vite build）。
      3) 应用工程 ProjectReference 到前端工程即可，targets 跨项目探测自动发现哪个引用是前端（前端工程自识别
         WebWindowUI.Frontend 标记并在被探针时回传自己路径；旧写法 WebWindowUIFrontend="true" 元数据仍可作显式声明）。
         后端模型库工程无需配置：桥 descriptor 默认写到「去掉 .Backend 后缀的兄弟前端工程」src（约定布局，见下）。

    前端构建：
      A. 自包含前端工程（推荐）：前端工程引用 WebWindowUI.Frontend 后，targets 的
         WebWindowUIBuildFrontend 目标（npm install / npm run build，Inputs/Outputs 增量）在
         .NET 构建时由应用工程 BuildFrontend 显式调用，vite 输出直接写进消费方产物目录的 wwwroot
         （BuildFrontend 把 $(TargetDir)\wwwroot 作为 WwwrootDir 传给前端工程）。wwwroot 资源项改为目标时注入——
         求值阶段 wwwroot 可能还不存在（前端构建恰好在 .NET 构建中途才产出它）。接法 A 下 .NET 构建会引入 Node/npm 执行。
         Release 分支：vite 产物改由前端工程自驱动（_WWUI_FrontendSelfDrive，CoreCompile 前）输出到
         前端工程 obj/.../wwwroot，再作为 EmbeddedResource 内嵌进前端 dll
         （_WWUI_EmbedWwwroot，LogicalName=wwwroot\<相对路径>，见目标注释）；应用不再直产/复制磁盘 wwwroot，
         WebResourceResolver 优先从 dll 内嵌资源读（Debug 仍走磁盘，见 WebResourceResolver.cs）。
      B. 手动前端：不提供前端构建工程，消费方自己把前端构建产物放进 wwwroot（求值阶段复制到输出目录），
         有 CheckFrontend 兜底报错。（Sample 用接法 A，见 WebWindowUI.Sample/WebWindowUI.Sample.Frontend/WebWindowUI.Sample.Frontend.csproj。）

    其余——生成器构建顺序、生成代码纳入编译——都在这里处理。

    路径约定（区分两套相对基准）：
      $(MSBuildThisFileDirectory)   = 本 targets 文件所在目录：仓库模式下是 WebWindowUI/，包模式下是包内 build/ →
                                      定位生成器（仓库=相邻源码工程，包=..\tools\net10.0\WebWindowUI.Generator.dll）；
      $(MSBuildProjectDirectory)    = 消费方工程目录 → 消费方的模型源文件 / wwwroot；
      $(WebWindowUIFrontendProject) = 前端工程文件（应用工程跨项目探测自动发现：对每个 ProjectReference 运行
                                      _WWUI_ReportFrontend，前端工程自识别标记并回传路径；构建期补齐，
                                      亦可显式 $(WebWindowUIFrontendProject) 覆盖）；
      $(WebWindowUIFrontendDir)     = 前端工程目录（前端内嵌或拆分布局均可，探测后补齐）；
      $(WebWindowUIBridgeDir)       = 桥 descriptor 输出目录（模型库工程零配置：默认 = 「去掉 .Backend 后缀的兄弟
                                      前端工程」目录，按 .Backend/.Frontend 兄弟布局约定推导；兄弟前端不存在回退
                                      本工程目录。显式声明优先，非标布局逃生舱）。
  -->

  <PropertyGroup>
    <!-- 防重复导入（三条导入链路，._WebWindowUITargetsLoaded 置位即后续导入跳过）：
         a) 包模式：NuGet buildTransitive 自动导入（消费方 PackageReference WebWindowUI 或标记包）；
         b) 仓库模式：根 Directory.Build.targets 导入（Import Condition 检查本标记，包模式先导入则跳过）；
         c) 历史写法：消费方工程 body 手动 <Import>（同样被 b) 的 Condition 避开）。
         Target 重复定义无害，ItemGroup 会真实重复，故靠标记属性避免 wwwroot 等 ItemGroup 重复注入。 -->
    <_WebWindowUITargetsLoaded Condition="'$(_WebWindowUITargetsLoaded)' == ''">true</_WebWindowUITargetsLoaded>
  </PropertyGroup>

  <!-- 跨平台 npm/vite 命令：targets 跑在消费方工程（实际构建宿主），用 IsOSPlatform 匹配构建机，
       不是 $(WWUIPlatform)（npm 命令跟着构建机走，即使交叉编译也只在本机跑前端构建）。
       Windows 用 .cmd（带扩展名自动经 cmd.exe 执行），非 Windows 用裸命令。
       注意条件不能写成 '$([MSBuild]::IsOSPlatform('Windows'))' == 'true'——嵌套单引号 MSB4092，
       函数返回 bool，裸调用即可（! 取反）。 -->
  <PropertyGroup>
    <WWUI_NpmCmd Condition="$([MSBuild]::IsOSPlatform('Windows'))">npm.cmd</WWUI_NpmCmd>
    <WWUI_NpmCmd Condition="!$([MSBuild]::IsOSPlatform('Windows'))">npm</WWUI_NpmCmd>
    <WWUI_ViteBin Condition="$([MSBuild]::IsOSPlatform('Windows'))">node_modules\.bin\vite.cmd</WWUI_ViteBin>
    <WWUI_ViteBin Condition="!$([MSBuild]::IsOSPlatform('Windows'))">node_modules\.bin\vite</WWUI_ViteBin>
  </PropertyGroup>

  <!-- 角色判定（求值期）：按引用项过滤标记库。
       WithMetadataValue 过滤 ItemGroup 内允许；PropertyGroup 条件不允许引用 item（MSB4099），
       故角色不落属性、直接以项列表信号贯穿：_WWUI_BackendRef / _WWUI_FrontendMarkerRef 非空即命中角色。
       精确匹配（而非子串）保证 WebWindowUI.Sample.Backend 这类「名字里带 Backend」的工程不误判为标记库。 -->
  <ItemGroup>
    <!-- 项目引用按 Filename（工程文件名）；包引用按 Identity（包名，Identity 精确匹配，
         因为包名无扩展名时 Filename 会把 .Backend 剥掉）。两个信号并集即角色证明。 -->
    <_WWUI_BackendRef Include="@(ProjectReference->WithMetadataValue('Filename', 'WebWindowUI.Backend'));@(PackageReference->WithMetadataValue('Identity', 'WebWindowUI.Backend'))" />
    <_WWUI_FrontendMarkerRef Include="@(ProjectReference->WithMetadataValue('Filename', 'WebWindowUI.Frontend'));@(PackageReference->WithMetadataValue('Identity', 'WebWindowUI.Frontend'))" />
  </ItemGroup>

  <!-- 平台实现（WebWindowUI.Platforms.{Windows,Linux,MacOS} 独立包）的引入完全走引用图，本文件不再注入：
       包模式：入口包 nuspec 带对应平台包依赖（打包时 -p:WWUI_PlatformRef=true 才写入），消费方只引
               WebWindowUI 即按系统自动带回平台包 + 平台自身依赖（WebView2 等）。
       仓库模式：入口工程 WebWindowUI.csproj 在 WWUI_PlatformRef != 'true' 时条件 ProjectReference 相邻平台
               工程，经引用图传递进消费方产物（测试工程另有自己的直接条件引用，见 WebWindowUI.Tests.csproj）。
       消费方 Main 首行调用 WebWindowUI.Platform.EnsureRegistered() 触发平台加载注册（AOT 安全：编译期静态引用）。 -->

  <!-- 桥 descriptor 输出目录默认（零配置）：工程名以 .Backend 结尾的模型库工程，默认写到「去掉 .Backend 后缀的
       兄弟前端工程」的 src。约定 App=<名>、后端=<名>.Backend、前端=<名>.Frontend 三工程同目录平铺（模板/样例/演示
       均此布局），GenerateModelProto 落桥文件到 $(WebWindowUIBridgeDir)\src\bridge 与 src\models。
       角色仍由引用证明（_WWUI_BackendRef），这里只解决「兄弟前端在哪」——后端不引用前端（探测探不到），故按框架
       自己强制的布局约定推导。显式 $(WebWindowUIBridgeDir) 始终优先（非标布局逃生舱）；兄弟前端不存在（非标命名/
       纯后端/接法 B 手动前端）时此约定置空，由 _WWUI_DiscoverFrontend 目标兜底 = 本工程目录。
       （PropertyGroup 条件不能引用 item 列表——MSB4099——故只用 .Backend 后缀名触发，不动 _WWUI_BackendRef。） -->
  <PropertyGroup>
    <_WWUI_ConventionBridgeDir Condition="'$(WebWindowUIBridgeDir)' == '' And $(MSBuildProjectName.EndsWith('.Backend'))">$(MSBuildProjectDirectory)/../$(MSBuildProjectName.Replace('.Backend','')).Frontend</_WWUI_ConventionBridgeDir>
    <_WWUI_ConventionBridgeDir Condition="'$(_WWUI_ConventionBridgeDir)' != '' And !Exists('$(_WWUI_ConventionBridgeDir)')"></_WWUI_ConventionBridgeDir>
    <WebWindowUIBridgeDir Condition="'$(_WWUI_ConventionBridgeDir)' != ''">$(_WWUI_ConventionBridgeDir)</WebWindowUIBridgeDir>
  </PropertyGroup>

  <PropertyGroup>
    <!-- 生成器定位，两种模式（消费方均可覆盖）：
         仓库模式（默认）：生成器是本库源码相邻的 WebWindowUI.Generator 工程，先构建再 dotnet <dll> 调用；
         包模式：生成器随 WebWindowUI 包内嵌在 tools/net10.0/。按存在性自动选择。 -->
    <WebWindowUIGeneratorProject Condition="'$(WebWindowUIGeneratorProject)' == ''">$(MSBuildThisFileDirectory)..\WebWindowUI.Generator\WebWindowUI.Generator.csproj</WebWindowUIGeneratorProject>
    <WebWindowUIGeneratorTargetFramework Condition="'$(WebWindowUIGeneratorTargetFramework)' == ''">net10.0</WebWindowUIGeneratorTargetFramework>
    <WebWindowUIGeneratorDll Condition="'$(WebWindowUIGeneratorDll)' == '' And Exists('$(MSBuildThisFileDirectory)..\tools\$(WebWindowUIGeneratorTargetFramework)\WebWindowUI.Generator.dll')">$(MSBuildThisFileDirectory)..\tools\$(WebWindowUIGeneratorTargetFramework)\WebWindowUI.Generator.dll</WebWindowUIGeneratorDll>
    <WebWindowUIGeneratorDll Condition="'$(WebWindowUIGeneratorDll)' == ''">$([System.IO.Path]::GetDirectoryName('$(WebWindowUIGeneratorProject)'))/bin/$(Configuration)/$(WebWindowUIGeneratorTargetFramework)/WebWindowUI.Generator.dll</WebWindowUIGeneratorDll>

  </PropertyGroup>

  <!-- 模型自动发现：仅后端模型库（引用 WebWindowUI.Backend 标记 → _WWUI_BackendRef 非空）扫描工程目录下
       以 Model.cs 结尾的源码（文件名即类名，递归含子目录，如 Items\NestedListItemModel.cs），ProtoBase 由类名
       推导（PascalCase → snake_case：MainWindowModel → main_window_model）。显式排除 obj/bin 下的中间产物
       （自定义 item 不套用 SDK 默认 Exclude，否则 ** 递归会把 bin/obj 里的拷贝也收进来）。
       显式声明过 ModelProtoFile 时不再自动扫描（可覆盖命名约定）。
       ItemGroup 条件可引用 item 列表（PropertyGroup 不行），故用 @(_WWUI_BackendRef) 判定角色。 -->
  <ItemGroup Condition="'@(_WWUI_BackendRef)' != '' And '@(ModelProtoFile)' == ''">
    <ModelProtoFile Include="**\*Model.cs" Exclude="obj\**\*Model.cs;bin\**\*Model.cs">
      <ProtoBase>$([System.Text.RegularExpressions.Regex]::Replace('%(ModelProtoFile.Filename)', '([a-z0-9])([A-Z])', '$1_$2').ToLowerInvariant())</ProtoBase>
    </ModelProtoFile>
  </ItemGroup>

  <!-- 前端工程定位：默认靠「跨项目探测」——对每个 ProjectReference 运行 _WWUI_ReportFrontend，前端工程
       （自识别：引用了 WebWindowUI.Frontend 标记，见 _WWUI_FrontendMarkerRef）返回自己的工程文件路径，其余返回空；
       MSBuild 任务的 TargetOutputs 即前端路径。探测是构建期操作（MSBuild 任务只能执行目标、求值期不可得），
       故 WebWindowUIFrontendProject / WebWindowUIFrontendDir / WebWindowUIBridgeDir 改为在 _WWUI_DiscoverFrontend
       目标里按需补齐（不再在求值期回退到本工程目录）。
       兼容旧写法：带 WebWindowUIFrontend="true" 元数据的 ProjectReference 仍在求值期定位（_WWUI_FrontendRef，
       求值期从 @(ProjectReference) 按自定义元数据过滤，WithMetadataValue 非条件故可引用自定义元数据），
       探测结果只在属性仍为空时生效 → 显式声明优先、探测兜底。两类工程共享定位结果：
         模型库工程（_WWUI_BackendRef 非空）继承前端目录作桥 descriptor 输出（WebWindowUIBridgeDir），本身不构建前端；
         应用工程（无角色标记）继承前端目录并取该 csproj 作构建入口（BuildFrontend → vite 直产本工程 wwwroot）。
       前端工程自身引用 WebWindowUI.Frontend 标记即激活 WebWindowUIBuildFrontend 目标，无需指向前端的引用。
       多个前端引用时 @(_WWUI_ProbedFrontend) 会带分号、MSBuild Projects 报错——可显式 $(WebWindowUIFrontendProject) 覆盖。 -->
  <ItemGroup Condition="'$(WebWindowUIFrontendProject)' == ''">
    <_WWUI_FrontendRef Include="@(ProjectReference->WithMetadataValue('WebWindowUIFrontend', 'true'))" />
    <_WWUI_FrontendPath Include="@(_WWUI_FrontendRef->'%(FullPath)')" />
    <!-- %(Directory) 不含盘符（绝对路径时去掉 RootDir，返回 code\...\Frontend\），须拼 %(RootDir) 得绝对目录。
         否则生成器 Exec 以相对路径（相对本工程 WorkingDirectory）落盘，descriptor 会写到本工程目录下的错误位置。 -->
    <_WWUI_FrontendDir Include="@(_WWUI_FrontendPath->'%(RootDir)%(Directory)')" />
  </ItemGroup>
  <PropertyGroup>
    <!-- 旧写法属性（显式声明优先）：WebWindowUIFrontend="true" 元数据定位前端；目录由 %(RootDir)%(Directory) 派生
         （属性函数参数不展开 item 引用，不能用 GetDirectoryName 叠，item transform 只在 Include 里展开）。
         无显式声明时这里不落值，交给 _WWUI_DiscoverFrontend 探测补齐。 -->
    <WebWindowUIFrontendProject Condition="'$(WebWindowUIFrontendProject)' == ''">@(_WWUI_FrontendPath)</WebWindowUIFrontendProject>
    <WebWindowUIFrontendDir Condition="'$(WebWindowUIFrontendDir)' == '' And '$(WebWindowUIFrontendProject)' != ''">@(_WWUI_FrontendDir)</WebWindowUIFrontendDir>
  </PropertyGroup>

  <!-- 跨项目探测：前端工程自识别并在被探针时返回自己工程文件路径（Identity = 完整路径）。
       应用类工程（无角色标记、自身已探测到前端引用）把发现的前端透传出去——前端发现沿引用链传递，
       任何引用应用工程的宿主（测试工程、二次应用）也能发现前端、在 Release 注入 FrontendLoad 强制加载
       （见 _WWUI_InjectFrontendLoad）。非前端且未发现前端的工程返回空 → TargetOutputs 为空，探测方跳过。
       透传分支 DependsOnTargets _WWUI_DiscoverFrontend：被探针时若自身还没探测过（子 MSBuild 进程首次），
       先探测一遍再回传（工程引用图无环，无递归问题）。
       SkipNonexistentTargets：消费方的 ProjectReference 里可能有未导入本 targets 的工程（标记库源码工程、
       生成器工程），探针目标不存在时静默跳过，不报 MSB4057。 -->
  <Target Name="_WWUI_ReportFrontend" DependsOnTargets="_WWUI_DiscoverFrontend" Returns="@(_WWUI_ReportedFrontend)">
    <ItemGroup Condition="'@(_WWUI_FrontendMarkerRef)' != ''">
      <_WWUI_ReportedFrontend Include="$(MSBuildProjectFullPath)" />
    </ItemGroup>
    <ItemGroup Condition="'@(_WWUI_FrontendMarkerRef)' == '' And '@(_WWUI_BackendRef)' == '' And '$(WebWindowUIFrontendProject)' != ''">
      <_WWUI_ReportedFrontend Include="$(WebWindowUIFrontendProject)" />
    </ItemGroup>
  </Target>

  <!-- 前端定位结果补齐：探测全部 ProjectReference 取回前端路径；按需填 WebWindowUIFrontendProject /
       WebWindowUIFrontendDir / WebWindowUIBridgeDir（消费方 PropertyGroup 覆盖过的保持原值）。
       属性在目标体里补齐（求值期探测结果不可得；目标体 PropertyGroup 里 @(Item) 会完全展开，无求值期字面量坑）。
       多入口 DependsOnTargets 共享本目标，MSBuild 对目标去重、每次构建只执行一次。 -->
  <Target Name="_WWUI_DiscoverFrontend">
    <MSBuild Projects="@(ProjectReference)" Targets="_WWUI_ReportFrontend" BuildInParallel="true"
             SkipNonexistentTargets="true" SkipNonexistentProjects="true" Properties="Configuration=$(Configuration)">
      <Output TaskParameter="TargetOutputs" ItemName="_WWUI_ProbedFrontend" />
    </MSBuild>
    <PropertyGroup>
      <WebWindowUIFrontendProject Condition="'$(WebWindowUIFrontendProject)' == '' And '@(_WWUI_ProbedFrontend)' != ''">@(_WWUI_ProbedFrontend)</WebWindowUIFrontendProject>
    </PropertyGroup>
    <PropertyGroup>
      <!-- 前端工程目录：默认 = 本工程目录（前端内嵌本工程）；有前端引用时取它所在目录（拆分布局）。
           无前端引用时回退本工程目录。 -->
      <WebWindowUIFrontendDir Condition="'$(WebWindowUIFrontendDir)' == '' And '$(WebWindowUIFrontendProject)' != ''">$([System.IO.Path]::GetDirectoryName('$(WebWindowUIFrontendProject)'))</WebWindowUIFrontendDir>
      <WebWindowUIFrontendDir Condition="'$(WebWindowUIFrontendDir)' == ''">$(MSBuildProjectDirectory)</WebWindowUIFrontendDir>
      <!-- 桥 descriptor 输出目录：GenerateModelProto 把 *.json/*.ts 写到这里（前端 src/bridge、src/models）。
           模型库工程已在求值期按「兄弟前端」约定落默认（见上 PropertyGroup），此处只兜底无角色标记的工程
           （应用等，实际上不跑 GenerateModelProto）→ 沿用前端工程目录。 -->
      <WebWindowUIBridgeDir Condition="'$(WebWindowUIBridgeDir)' == ''">$(WebWindowUIFrontendDir)</WebWindowUIBridgeDir>
    </PropertyGroup>
  </Target>

  <!-- ReferenceOutputAssembly 移入库：前端工程对应用是「纯构建顺序 + wwwroot 载体」——Debug 不引用其 dll
       （wwwroot 走磁盘直产，见 BuildFrontend），只有 Release 才引用（wwwroot 已内嵌进前端 dll，应用产物目录
       需带上它供 WebResourceResolver 扫描，见 _WWUI_EmbedWwwroot）。旧写法在消费方 csproj 里写
       <ReferenceOutputAssembly>$([System.String]::Equals(...))</ReferenceOutputAssembly>，现由本目标按探测结果
       （_WWUI_ProbedFrontend）在构建期 Update 到前端 ProjectReference 上。
       必须挂 AssignProjectConfiguration 之前：SDK 在那里把 @(ProjectReference) 拷进 ProjectReferenceWithConfiguration
       （_SplitProjectReferencesByFileExistence 再转 _MSBuildProjectReferenceExistent，ResolveProjectReferences
       只读 %(_MSBuildProjectReferenceExistent.ReferenceOutputAssembly) 决定是否引用/复制）——挂晚了元数据已快照，
       改不到（实测 Debug 前端 dll 仍进应用 bin）。其余两个挂点是兜底（AssignProjectConfiguration 有条件跳过时）。 -->
  <Target Name="_WWUI_ApplyFrontendRefMetadata" Condition="'@(_WWUI_FrontendMarkerRef)' == ''"
          DependsOnTargets="_WWUI_DiscoverFrontend"
          BeforeTargets="AssignProjectConfiguration;_SplitProjectReferencesByFileExistence;ResolveProjectReferences">
    <ItemGroup>
      <ProjectReference Update="@(ProjectReference)"
                        Condition="'%(ProjectReference.FullPath)' == '@(_WWUI_ProbedFrontend)'"
                        ReferenceOutputAssembly="$([System.String]::Equals('$(Configuration)','Release'))" />
    </ItemGroup>
  </Target>

  <!-- 生成器构建顺序：不引用其程序集，只保证 Generator 先构建好（有模型声明时）。
       ReferenceOutputAssembly=false 只阻止主 dll 进引用，Exe 工程输出的 apphost.exe/runtimeconfig.json/deps.json
       仍会经 GetCopyToOutputDirectoryItems 透传复制进消费方 bin（→ Backend/Frontend/应用 bin 都出现
       WebWindowUI.Generator.exe）——加 Private="false" 阻断该复制（SDK 的透传条件见
       Microsoft.Common.CurrentVersion.targets _GetCopyToOutputDirectoryItemsFromTransitiveProjectReferences），
       构建排序不受影响。 -->
  <ItemGroup Condition="'@(ModelProtoFile)' != '' And Exists('$(WebWindowUIGeneratorProject)')">
    <ProjectReference Include="$(WebWindowUIGeneratorProject)" ReferenceOutputAssembly="false" Private="false" />
  </ItemGroup>

  <!-- 前端工程自身构建目标（引用 WebWindowUI.Frontend 标记 → _WWUI_FrontendMarkerRef 非空即激活；
       由应用工程 BuildFrontend 以 Targets="WebWindowUIBuildFrontend" 显式调用，WwwrootDir 经 Properties 传入）：
       - wwwroot 完整且前端源码未变时跳过 vite build（Inputs/Outputs 增量）；
       - node_modules 缺失时才 npm install。
       npm 约定：package.json 提供 run build（vite build），输出目录经 WWWROOT_DIR 环境变量给 vite.config.ts。
       单独构建前端工程（SDK 默认 Build）是空操作（无 C#），vite 只在应用构建编排时被显式调用。 -->
  <PropertyGroup>
    <!-- WwwrootDir（前端 vite 输出目录）默认值，消费方不再写覆盖：
         Debug 由应用 BuildFrontend 经 MSBuild Properties 传入 $(TargetDir)wwwroot（直产应用 bin，不落此默认）；
         Release 前端自驱动时默认输出到本工程 obj\...\wwwroot，供 _WWUI_EmbedWwwroot 内嵌进前端 dll；
         未进任何构建编排（手动 npm run build）时落本工程 wwwroot。
         注意不能直接用 $(IntermediateOutputPath)：本 PropertyGroup 在工程 body 里求值，而 SDK 的
         IntermediateOutputPath（带 TargetFramework 段）在 Sdk.targets 里才定义（body 之后导入），
         求值时仍是空 → $(IntermediateOutputPath)wwwroot 会塌缩成相对 wwwroot（vite 把产物写进工程根
         源码目录）。故用求值点已就绪的分量拼出同一路径：BaseIntermediateOutputPath（Microsoft.Common.props
         =obj\，Sdk.props 先导入）、Configuration（全局）、TargetFramework（工程 body 已写）。 -->
    <WwwrootDir Condition="'$(WwwrootDir)' == '' And '$(Configuration)' == 'Release'">$(BaseIntermediateOutputPath)$(Configuration)\$(TargetFramework)\wwwroot</WwwrootDir>
    <WwwrootDir Condition="'$(WwwrootDir)' == ''">$(MSBuildProjectDirectory)\wwwroot</WwwrootDir>
  </PropertyGroup>
  <ItemGroup Condition="'@(_WWUI_FrontendMarkerRef)' != ''">
    <!-- 增量输入：前端源码 + 构建配置（任一更新触发重建）。桥库是 npm 依赖，版本变更经 package.json 体现。 -->
    <FrontendInput Include="src\**\*" />
    <FrontendInput Include="public\**\*" />
    <FrontendInput Include="package.json" />
    <FrontendInput Include="package-lock.json" Condition="Exists('package-lock.json')" />
    <FrontendInput Include="vite.config.ts" />
    <FrontendInput Include="tsconfig.json" Condition="Exists('tsconfig.json')" />
    <!-- 增量输出：各窗口页面 index.html + 构建成功标记。
         标记字面路径保证 Outputs 非空——首次构建 wwwroot 尚不存在时 glob 为空，目标会被当 up-to-date 跳过。 -->
    <FrontendOutput Include="$(WwwrootDir)\window\**\index.html" />
    <FrontendOutput Include="$(WwwrootDir)\.frontend-stamp" />
  </ItemGroup>
  <!-- 安装依赖：node_modules 里没有 vite 时才跑。命令/可执行路径按构建机选（Windows=npm.cmd/vite.cmd，其余裸命令）。 -->
  <Target Name="EnsureNodeModules" Condition="'@(_WWUI_FrontendMarkerRef)' != '' And !Exists('$(MSBuildProjectDirectory)\$(WWUI_ViteBin)')">
    <Exec Command="$(WWUI_NpmCmd) install" WorkingDirectory="$(MSBuildProjectDirectory)" />
  </Target>
  <!-- 构建：vite build（输出目录经 WWWROOT_DIR、构建配置经 WWUI_CONFIGURATION 两个环境变量传给 vite.config.ts，
       后者按配置区分产物——Debug 不压缩/不混淆便于开发直接看，Release 才 minify）。构建成功后写标记，
       下次构建靠标记与 Inputs 比时间戳决定是否跳过。 -->
  <Target Name="WebWindowUIBuildFrontend" Condition="'@(_WWUI_FrontendMarkerRef)' != ''" DependsOnTargets="EnsureNodeModules" Inputs="@(FrontendInput)" Outputs="@(FrontendOutput)">
    <Exec Command="$(WWUI_NpmCmd) run build" WorkingDirectory="$(MSBuildProjectDirectory)" EnvironmentVariables="WWWROOT_DIR=$(WwwrootDir);WWUI_CONFIGURATION=$(Configuration)" />
    <Touch Files="$(WwwrootDir)\.frontend-stamp" AlwaysCreate="true" />
  </Target>

  <!-- 接法 B（无前端构建工程）：wwwroot 资源原样复制到输出目录。不再内嵌进程序集——
       wwwroot 统一放产物目录，WebResourceResolver 从磁盘读取。排除前端工程自身（_WWUI_FrontendMarkerRef 非空）。
       原在求值期处理，但条件里的 $(WebWindowUIFrontendProject) 改由探测在构建期补齐（求值期恒空），
       故移进目标体、DependsOn 探测之后判断：探测未发现前端工程才走磁盘 wwwroot。
       本目标也顺带作为应用工程的探测入口（BeforeTargets=PrepareForBuild 最早触发 _WWUI_DiscoverFrontend）。 -->
  <Target Name="_WWUI_AddManualWwwrootContent" BeforeTargets="PrepareForBuild"
          Condition="'@(_WWUI_FrontendMarkerRef)' == '' And '$(_WebWindowUITargetsLoaded)' == 'true'"
          DependsOnTargets="_WWUI_DiscoverFrontend">
    <ItemGroup Condition="'$(WebWindowUIFrontendProject)' == '' And Exists('$(MSBuildProjectDirectory)\wwwroot')">
      <Content Include="$(MSBuildProjectDirectory)\wwwroot\**" CopyToOutputDirectory="PreserveNewest" />
    </ItemGroup>
  </Target>

  <!-- 接法 B 兜底：声明了模型（意图作为前端工程）且已有 wwwroot 却缺前端窗口页面时直接报错。
       后端模型库（如 Sample 的 WebWindowUI.Sample.Backend：有模型但无前端工程/无 wwwroot）即使被自动导入 targets 也不报错——空操作。
       探测结果（WebWindowUIFrontendProject）构建期才就绪，故从目标 Condition 移到 Error Condition（DependsOn 探测先跑）。 -->
  <Target Name="CheckFrontend" Condition="'@(ModelProtoFile)' != '' And Exists('$(MSBuildProjectDirectory)\wwwroot')"
          DependsOnTargets="_WWUI_DiscoverFrontend" BeforeTargets="PrepareForBuild">
    <Error Condition="'$(WebWindowUIFrontendProject)' == '' And !Exists('$(MSBuildProjectDirectory)\wwwroot\window')" Text="未找到前端构建产物（wwwroot\window 缺失）。请先在前端工程目录执行 npm install 与 npm run build，或按接法 A 提供前端构建工程。" />
  </Target>

  <!-- 模型 → proto 前端文件生成：每个模型一个条目（文件名即类名，生成器按文件名推类名），
       按条目批处理执行一次 dotnet 生成调用。
       C# DTO（{Model}Proto.g.cs）已改由源生成器 WebWindowUI.Generator.SourceGen.ProtoGenerator
       内存产出（与模型同程序集），本目标只落盘前端文件：
       + $(WebWindowUIBridgeDir)\src\bridge\{ProtoBase}.json（前端 descriptor 用，
       写进前端工程的 src/bridge；模型库工程按「去掉 .Backend 后缀的兄弟前端工程」约定自动定位
       WebWindowUIBridgeDir，无需配置——见上。Sample 里模型在 WebWindowUI.Sample.Backend、
       前端工程是兄弟目录 WebWindowUI.Sample.Frontend，descriptor 自动落到那里）
       + $(WebWindowUIBridgeDir)\src\models\{命名空间子路径}\{Model}.ts（前端 TS 模型镜像，
       子路径由模型命名空间去掉「全部模型命名空间的公共前缀」推导，剩余段小写，如 WebWindowUI.Sample.Users → users/）。
       根命名空间不再手工配置：全模型清单（all-models 参数）传入全部模型文件，生成器自动取公共前缀（root-namespace 可显式覆盖）。
       每次构建都执行（不用 Inputs/Outputs 增量）：生成器做幂等写入（内容相同不写文件、保持 mtime），
       故 descriptor/TS 缺失时必重建（增量 Outputs 若只追踪 obj 里的 .g.cs，前端 descriptor 缺失时会被误判
       「目标最新」而跳过，导致前端 vite 构建 import 不到 descriptor 而失败），内容不变时文件时间戳不动、
       前端增量（FrontendInput 含 src/bridge、src/models）不被打扰。
       不设 Inputs/Outputs 也绕开了 MSB4012：桥 descriptor 目标目录 WebWindowUIBridgeDir 由 ProjectReference 项
       的 %(RootDir)%(Directory) 派生、以纯 @(Item) 进属性，MSBuild 的 Outputs 是 item 感知上下文、属性值里的
       item 引用拼接字符串会报错，而 Exec/条件等非 item 上下文能递归展开——桥文件天然只能走目标体、不能进 Outputs。 -->
  <!-- 全模型清单落成属性：GenerateModelProto 以 %(ModelProtoFile.Identity) 批处理（model 参数），
       MSBuild 会把目标体内对 @(ModelProtoFile) 的引用限定到当前批次（只有当前模型），
       all-models 参数若用同一 transform 只会传单个路径，typed List<模型> 检测会因缺元素模型源码而回退 ModelValue。
       故先在独立目标（无批处理元数据引用）把全量路径 transform 转成项、再以裸 @(Item) 进属性，
       Exec 里引用属性（属性不参与批处理作用域）。裸 @(Item) 进属性求值期展开，与前端路径定位同一约定。 -->
  <Target Name="_WWUI_ComputeAllModels" Condition="'@(_WWUI_BackendRef)' != '' And '@(ModelProtoFile)' != ''">
    <ItemGroup>
      <_WWUI_AllModelPaths Include="@(ModelProtoFile->'$(MSBuildProjectDirectory)/%(Identity)')" />
    </ItemGroup>
    <PropertyGroup>
      <WebWindowUIAllModelSources>@(_WWUI_AllModelPaths)</WebWindowUIAllModelSources>
    </PropertyGroup>
  </Target>
  <!-- 残留文件清理（生成前）：删除「当前模型集合不再产出的」旧桥 descriptor（改名/删除模型留下的孤儿文件）。
       只剪枝不复写：生成器 WriteIfChanged 幂等（内容不变保持 mtime）→ 原地文件不重写、不打扰前端 vite 增量
       （FrontendInput 含 src/bridge、src/models）。若整目录删除再全量重写，每次构建都刷所有文件时间戳 →
       每次都触发 vite 重建，幂等优化失效。
       桥 descriptor 平铺（src\bridge\{ProtoBase}.json），按名剪即可（ProtoBase 即生成器取 ProtoBase 的推导值）；
       TS 镜像带命名空间子路径（src\models\{子路径}\{类名}.ts），「任意子路径按类名排除」会把换命名空间后的
       旧路径文件漏掉（同名不同路径），改由生成器按「类名 → 期望子路径」精确剪
       （见 WebWindowUI.Generator/Program.cs PruneStaleTs，仅 all-models 参数缺失时不剪）。未命中的文件即孤儿，
       Delete 清除（空 glob 无害）。 -->
  <Target Name="_WWUI_CleanBridgeOutputs"
          Condition="'@(_WWUI_BackendRef)' != ''">
    <ItemGroup>
      <_WWUI_StaleBridge Include="$(WebWindowUIBridgeDir)\src\bridge\*.json"
                         Exclude="@(ModelProtoFile->'$(WebWindowUIBridgeDir)\src\bridge\%(ProtoBase).json')" />
    </ItemGroup>
    <Delete Files="@(_WWUI_StaleBridge)" />
  </Target>

  <Target Name="GenerateModelProto"
          Condition="'@(_WWUI_BackendRef)' != '' And '@(ModelProtoFile)' != ''"
          DependsOnTargets="_WWUI_DiscoverFrontend;_WWUI_ComputeAllModels;_WWUI_CleanBridgeOutputs"
          BeforeTargets="CoreCompile">
    <Exec Command="dotnet &quot;$(WebWindowUIGeneratorDll)&quot; --model &quot;$(MSBuildProjectDirectory)/%(ModelProtoFile.Identity)&quot; --json-out &quot;$(WebWindowUIBridgeDir)/src/bridge/%(ModelProtoFile.ProtoBase).json&quot; --ts-out-dir &quot;$(WebWindowUIBridgeDir)/src/models&quot; --all-models &quot;$(WebWindowUIAllModelSources)&quot;" />
  </Target>

  <!-- 生成代码纳入编译不再需要目标：{Model}Proto.g.cs 已由源生成器（WebWindowUI.Generator.SourceGen.ProtoGenerator）
       内存产出、与 WriteBack.g.cs 一并进编译，obj/generated 不再落 C#。 -->

  <!-- 接法 A：应用工程（无角色标记 → _WWUI_BackendRef / _WWUI_FrontendMarkerRef 均空）按需构建前端工程
       （前端工程 WebWindowUIBuildFrontend 自管增量：wwwroot 完整且前端源码未变时跳过 npm 调用）。
       DependsOnTargets GenerateModelProto：桥 descriptor（*.json）生成后再构建前端，
       模型改动 → descriptor 更新 → 前端 Input 变化 → 触发前端重建。
       注意 GenerateModelProto 是 BeforeTargets="CoreCompile"（生成器工程在 ResolveReferences 才构建好），
       此目标经 DependsOn 挂在它之后，仍落在 CoreCompile 之前。
       后端工程也有 WebWindowUIFrontendProject（只继承前端目录作 bridge 输出），不构建前端——_WWUI_BackendRef 非空排除。
       Release 下短路：wwwroot 改由前端工程自驱动 vite 并内嵌进其 dll（见 _WWUI_FrontendSelfDrive），
       应用不再直产/复制磁盘 wwwroot。 -->
  <Target Name="BuildFrontend"
          Condition="'$(Configuration)' != 'Release' And '@(_WWUI_BackendRef)' == '' And '@(_WWUI_FrontendMarkerRef)' == ''"
          DependsOnTargets="_WWUI_DiscoverFrontend;GenerateModelProto">
    <!-- 把消费方产物目录的 wwwroot 路径传给前端工程当输出目录：vite 直接写进消费方 bin，
         不经过消费方工程下的 wwwroot 中转（Sample 的 wwwroot 直接落在应用工程产物目录）。
         探测结果构建期才就绪，不能进目标 Condition（条件在 DependsOnTargets 之前求值），放 MSBuild 调用上。 -->
    <MSBuild Condition="'$(WebWindowUIFrontendProject)' != ''"
             Projects="$(WebWindowUIFrontendProject)" Targets="WebWindowUIBuildFrontend"
             Properties="WwwrootDir=$(TargetDir)wwwroot" />
  </Target>

  <!-- Release 前端自驱动 + 内嵌（前端工程自身，_WWUI_FrontendMarkerRef 非空）：
       _WWUI_FrontendSelfDrive：CoreCompile 前自跑 vite（WebWindowUIBuildFrontend 增量），输出到
       前端工程 obj/.../wwwroot（由前端工程 PropertyGroup 覆盖 WwwrootDir）；
       _WWUI_EmbedWwwroot：把 $(WwwrootDir)\** 注入 EmbeddedResource 编进前端 dll。
       挂点选 BeforeTargets="_GenerateCompileInputs"（Compile 链、CoreCompile 之前）而非 BeforeTargets=CoreCompile：
       SDK 在 _GenerateCompileInputs 里才把 @(EmbeddedResource)（Type=Non-Resx 且 WithCulture=false）组装成
       Csc 的 Resources 参数（_CoreCompileResourceInputs，见 Microsoft.Common.CurrentVersion.targets），
       它在 CoreCompile 依赖链中先于 BeforeTargets=CoreCompile 的目标执行——挂 CoreCompile 会让 Csc 收不到本目标注入的项。
       LogicalName 约定 = wwwroot\<相对路径>（如 wwwroot\window\main\index.html，RecursiveDir 保留反斜杠），
       与运行时 WebResourceResolver 的内嵌查找（relativePath.Replace('/', '\\') 后加 wwwroot\ 前缀）对称。
       元数据值里的 %(RecursiveDir)/%(Filename)/%(Extension) 是普通元数据引用、逐项求值（同 AddWebWindowUIResourcesCopy
       的 TargetPath 写法）；但元数据函数（%(RecursiveDir.Replace(...))）在项元数据值里不展开、会当字面量保留，
       故不用点号约定。Exclude .frontend-stamp（构建标记，不是页面资源）。
       Debug 下不触发（磁盘直产走 BuildFrontend）。 -->
  <Target Name="_WWUI_FrontendSelfDrive"
          Condition="'@(_WWUI_FrontendMarkerRef)' != '' And '$(Configuration)' == 'Release'"
          DependsOnTargets="WebWindowUIBuildFrontend" />
  <Target Name="_WWUI_EmbedWwwroot" BeforeTargets="_GenerateCompileInputs"
          Condition="'@(_WWUI_FrontendMarkerRef)' != '' And '$(Configuration)' == 'Release'"
          DependsOnTargets="_WWUI_FrontendSelfDrive">
    <ItemGroup Condition="Exists('$(WwwrootDir)')">
      <_WWUI_EmbedFiles Include="$(WwwrootDir)\**" Exclude="$(WwwrootDir)\.frontend-stamp" />
    </ItemGroup>
    <ItemGroup Condition="'@(_WWUI_EmbedFiles)' != ''">
      <EmbeddedResource Include="@(_WWUI_EmbedFiles)">
        <LogicalName>wwwroot\%(_WWUI_EmbedFiles.RecursiveDir)%(_WWUI_EmbedFiles.Filename)%(_WWUI_EmbedFiles.Extension)</LogicalName>
        <Type>Non-Resx</Type>
        <WithCulture>false</WithCulture>
      </EmbeddedResource>
    </ItemGroup>
  </Target>

  <!-- 前端 dll 静态引用注入（Release，AOT 安全）：内嵌 wwwroot 的前端 dll 是纯 Vue 工程产物、
       本身无任何 C# 类型，无法按类型静态引用；NativeAOT 又没有运行时按名加载。
       故改为两端注入可静态引用的宿主类型（源文件随包分发在 build/ 与 buildTransitive/，
       $(MSBuildThisFileDirectory) = targets 所在目录，仓库模式 = WebWindowUI/）：
         _WWUI_InjectFrontendHost：前端工程（引用 WebWindowUI.Frontend 标记）注入 FrontendHost.cs，
           给前端 dll 一个可引用类型；
         _WWUI_InjectFrontendLoad：应用工程（无角色标记 + 探测到前端引用）注入 FrontendLoad.cs，
           [ModuleInitializer] 在进程启动时 typeof(FrontendHost)——JIT 下强制加载前端 dll
           （进入已加载程序集）、NativeAOT 下根进链接闭包（内嵌 wwwroot 随之保留）。
           「探测到前端引用」= WebWindowUIFrontendProject 非空；前端发现经 _WWUI_ReportFrontend 沿引用链
           传递（见该目标注释），故测试工程等经应用工程间接引用前端的宿主也会注入（测试进程不加载应用 dll，
           只有本 dll 自身被加载才能触发前端加载）。
       WebResourceResolver 由此只扫已加载程序集即命中，无需运行时按名加载。
       挂 BeforeTargets="_GenerateCompileInputs"（同 _WWUI_EmbedWwwroot：SDK 在该处组装 Csc 的
       Compile/Resources 参数），Compile 项在目标体注入（角色/前端探测结果构建期才就绪）。 -->
  <Target Name="_WWUI_InjectFrontendHost" BeforeTargets="_GenerateCompileInputs"
          Condition="'@(_WWUI_FrontendMarkerRef)' != '' And '$(Configuration)' == 'Release'">
    <ItemGroup>
      <Compile Include="$(MSBuildThisFileDirectory)FrontendHost.cs" Link="FrontendHost.cs" />
    </ItemGroup>
  </Target>
  <Target Name="_WWUI_InjectFrontendLoad" BeforeTargets="_GenerateCompileInputs"
          Condition="'@(_WWUI_BackendRef)' == '' And '@(_WWUI_FrontendMarkerRef)' == '' And '$(Configuration)' == 'Release'"
          DependsOnTargets="_WWUI_DiscoverFrontend">
    <ItemGroup Condition="'$(WebWindowUIFrontendProject)' != ''">
      <Compile Include="$(MSBuildThisFileDirectory)FrontendLoad.cs" Link="FrontendLoad.cs" />
    </ItemGroup>
  </Target>

  <!-- 接法 A：目标时注入 wwwroot 资源项。前端构建已把 vite 产物直接写进 $(OutDir)\wwwroot（BuildFrontend
       把 $(TargetDir)\wwwroot 作为 WwwrootDir 传给前端工程），此目标再把这些文件声明成
       ContentWithTargetPath——复制管线真正消费的项，配 TargetPath 落到 $(OutDir)\wwwroot。
       父工程经 ProjectReference 拉子工程复制项时同样触发，保证测试工程也能收到 wwwroot 传递复制。
       不能走 Content→AssignTargetPaths 老路：AssignTargetPaths 一次构建只跑一次（在 PrepareResources 阶段，
       那时 wwwroot 还没产出）。故直接注入 ContentWithTargetPath。
       挂 BeforeTargets="GetCopyToOutputDirectoryItems"（CoreCompile 之后、复制时触发）。
       wwwroot 存在性检查放在目标体 ItemGroup 上（目标自身 Condition 在 DependsOnTargets 之前求值，放这里才能看到前端刚产出的 wwwroot）。
       注意：glob 先收进临时项 _WwwrootFiles，再以限定元数据注入目标项。直接 <ItemType Include=glob><TargetPath>…%(RecursiveDir)…
       是目标内 ItemGroup 的元数据自引用（MSB4120），会把预先存在的同类型项元数据交叉应用到新项上（此前把全部
       TargetPath 污染成 wwwroot\WebView2Loader.dll 的教训）。
       Release 下短路：wwwroot 已内嵌进前端 dll（_WWUI_EmbedWwwroot），输出目录不再保留磁盘 wwwroot。 -->
  <Target Name="AddWebWindowUIResourcesCopy"
          Condition="'$(Configuration)' != 'Release' And '@(_WWUI_BackendRef)' == '' And '@(_WWUI_FrontendMarkerRef)' == ''"
          DependsOnTargets="BuildFrontend"
          BeforeTargets="GetCopyToOutputDirectoryItems">
    <ItemGroup Condition="Exists('$(OutDir)\wwwroot')">
      <_WwwrootFiles Include="$(OutDir)\wwwroot\**" Exclude="$(OutDir)\wwwroot\.frontend-stamp" />
    </ItemGroup>
    <ItemGroup Condition="'@(_WwwrootFiles)' != ''">
      <ContentWithTargetPath Include="@(_WwwrootFiles)">
        <TargetPath>wwwroot\%(_WwwrootFiles.RecursiveDir)%(_WwwrootFiles.Filename)%(_WwwrootFiles.Extension)</TargetPath>
        <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      </ContentWithTargetPath>
    </ItemGroup>
  </Target>

</Project>
